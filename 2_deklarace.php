<?php
// Blok cvičení 01 - Proměnné a výstupy
// Cvičení 2 - Deklarace
//
// Kde výstup zkontrolujete: v konzoli (postup je v README.md).
// Konvence: názvy proměnných anglicky, camelCase, bez diakritiky.


/* --- ÚKOL 1 --- [hodnoceno automaticky]
 *
 * Deklarujte proměnnou `$name`, jejíž hodnotou bude vaše jméno.
 * Vypište ji ve formátu `Jmeno: <hodnota>`.
 *
 * Očekávaný výstup (s vaším jménem):
 * Jmeno: Karel
 */


/* --- ÚKOL 2 --- [hodnoceno automaticky]
 *
 * Deklarujte čtyři proměnné a nastavte jim hodnoty tak, aby odpovídaly
 * datovému typu v názvu:
 *   `$integerValue`  - celé číslo
 *   `$floatValue`    - desetinné číslo
 *   `$booleanValue`  - pravdivostní hodnota
 *   `$stringValue`   - text
 *
 * Skutečný datový typ každé proměnné pak ověřte funkcí `var_dump()`.
 * Pozor: `var_dump()` vypisuje typ, nejen hodnotu - právě proto ji používáme.
 *
 * Očekávaný výstup (hodnoty budou vaše, typy v závorkách musí být tyto):
 * int(42)
 * float(3.14)
 * bool(true)
 * string(4) "text"
 */


/* --- ÚKOL 3 --- [výstup hodnocen automaticky, odpověď kontroluje učitel]
 *
 * Deklarujte proměnnou `$nothing` a nastavte jí hodnotu `null`.
 * Vypište ji funkcí `var_dump()`.
 *
 * V komentáři pak odpovězte: jaký je rozdíl mezi proměnnou s hodnotou `null`
 * a proměnnou, kterou jsme vůbec nedeklarovali?
 *
 * Očekávaný výstup:
 * NULL
 *
 * Odpověď:
 */


/* --- ÚKOL 4 --- [hodnoceno automaticky]
 *
 * Deklarujte konstantu `SCHOOL_YEAR` s hodnotou `2026`.
 * Konstanta se zapisuje klíčovým slovem `const`, píše se VELKÝMI písmeny
 * a na rozdíl od proměnné nemá na začátku znak `$`.
 *
 * Vypište ji ve formátu `Skolni rok: 2026`.
 *
 * Očekávaný výstup:
 * Skolni rok: 2026
 */


/* --- ÚKOL 5 --- [nehodnoceno automaticky]
 *
 * Experiment: pod tento komentář zkuste konstantě `SCHOOL_YEAR` přiřadit
 * jinou hodnotu a soubor spusťte. Do komentáře zapište, co PHP udělalo.
 *
 * POZOR: řádek s přiřazením potom ZAKOMENTUJTE. Chyba, kterou uvidíte, je
 * totiž chyba syntaxe - PHP soubor kvůli ní vůbec nespustí a nevypíše ani
 * úkoly 1 až 4. Kdybyste ten řádek v souboru nechali, přijdete o body
 * za celé toto cvičení.
 *
 * Co se stalo:
 */
