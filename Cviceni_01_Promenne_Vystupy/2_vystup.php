<?php
// Blok cvičení 01 - Proměnné a výstupy

/*
 * Deklarujte proměnnou A, B
 * Pro proměnné A a B udělejte script, který provede matematickou operaci sčítání, násobení a každý výsledek uloží do samostatné proměnné.
 * Každý výsledek zobrazte na samostatném řádku.
 */

/*
 * Deklarujte:
 * Proměnná alfa obsahuje hodnotu 200
 * Proměnná beta obsahuje hodnotu 350
 * Proměnná gama obsahuje hodnotu 100
 *
 * Vhodným způsobem zaměňte obsah hodnot
 * Proměnná alfa obsahuje hodnotu beta
 * Proměnná beta obsahuje hodnotu alfa
 * Proměnná gama obsahuje hodnotu beta plus gama
 * 
 * Výsledek ověřte s konečným stavem:
 * alfa = 350
 * beta = 200
 * gama = 300
 * */
