<?php
// Blok cvičení 01 - Proměnné a výstupy
// Cvičení 4 - Spojování řetězců
//
// Kde výstup zkontrolujete: úkoly 1-3 v konzoli, úkol 4 v prohlížeči
// (postup je v README.md).
// Konvence: názvy proměnných anglicky, camelCase, bez diakritiky.


/* --- ÚKOL 1 --- [výstup hodnocen automaticky, odpověď kontroluje učitel]
 *
 * Deklarujte proměnné `$firstName` (vaše jméno) a `$lastName` (vaše příjmení).
 * Stejnou větu pak vypište TŘEMI různými způsoby, každý na samostatný řádek
 * a označený písmenem varianty:
 *
 *   a) spojováním řetězců operátorem `.`
 *   b) vložením proměnné do textu v dvojitých uvozovkách (tzv. interpolace)
 *   c) funkcí `sprintf()` se zástupným znakem `%s`
 *
 * Očekávaný výstup (s vaším jménem):
 * a) Jmenuji se Karel Novak a studuji informatiku.
 * b) Jmenuji se Karel Novak a studuji informatiku.
 * c) Jmenuji se Karel Novak a studuji informatiku.
 *
 * V komentáři odpovězte, která varianta se vám čte nejlépe a proč.
 *
 * Odpověď:
 */


/* --- ÚKOL 2 --- [hodnoceno automaticky]
 *
 * Deklarujte proměnnou `$subject` a sestavte výstup
 * `Vitejte na obsahu webu <subjekt>`, kde `<subjekt>` bude hodnota proměnné.
 *
 * Text věty napište v kódu JEN JEDNOU. Vyzkoušejte ho na třech různých
 * společnostech nebo stránkách tak, že budete měnit pouze hodnotu proměnné
 * a soubor znovu spustíte - nekopírujte celý příkaz třikrát pod sebe.
 * V souboru tedy nakonec zůstane jen poslední hodnota.
 *
 * Očekávaný výstup (s vaší hodnotou):
 * Vitejte na obsahu webu Obchodni akademie
 */


/* --- ÚKOL 3 --- [hodnoceno automaticky]
 *
 * Deklarujte proměnnou `$content` s hodnotou `PHP`.
 * Pak k ní dvěma dalšími příkazy postupně přidejte text ` je` a ` skvele.`
 * pomocí operátoru `.=` (přiřazení se spojením).
 * Nakonec proměnnou vypište.
 *
 * Očekávaný výstup:
 * PHP je skvele.
 */


/* --- ÚKOL 4 --- [hodnoceno automaticky]
 *
 * Obsah proměnné `$content` z úkolu 3 vypište ještě jednou, tentokrát
 * obalený v HTML značce odstavce. Výstup si otevřete v prohlížeči.
 *
 * Očekávaný výstup (ve zdrojovém kódu stránky):
 * <p>PHP je skvele.</p>
 */
